<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Bookie!</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/">Home</a> <span class="sr-only">(current)</span></a>
            </li>

            <% if (authorized) { %>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Reservation
              </a>
          
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="/reservations/new">Make a reservation</a>
                <a class="dropdown-item" href="/reservations">See all reservations</a>
              </div>
            </li>
            <% } %>

          </ul>


          <form class="form-inline my-2 my-lg-0">

              <ul class="navbar-nav mr-auto">

            <% if (!authorized) { %>

              
              <li class="nav-item active">
                <a class="nav-link" href="/login">Login</a> <span class="sr-only">(current)</span></a>
              </li>
              <% } %>

              <% if (!authorized) { %>

              <li class="nav-item active">
                  <a class="nav-link" href="/users/new">Register</a> <span class="sr-only">(current)</span></a>
                </li>
              <% } %>

              <% if (authorized) { %>
                <li class="nav-item active">
                  <a class="nav-link" href="/logout">Logout</a> <span class="sr-only">(current)</span></a>
                </li>
                <% } %>
  
              </ul>                
  
  
          
          </form>
        </div>
      </nav>